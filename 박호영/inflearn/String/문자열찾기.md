## 문자열 찾기 - https://cote.inflearn.com/contest/10/problem/01-01

```JAVA
/**
 * 설명
 한 개의 문자열을 입력받고, 특정 문자를 입력받아 해당 특정문자가 입력받은 문자열에 몇 개 존재하는지 알아내는 프로그램을 작성하세요.
 대소문자를 구분하지 않습니다.문자열의 길이는 100을 넘지 않습니다.

 입력
 첫 줄에 문자열이 주어지고, 두 번째 줄에 문자가 주어진다.
 문자열은 영어 알파벳으로만 구성되어 있습니다.

 출력
 첫 줄에 해당 문자의 개수를 출력한다.
 */

import java.util.*;

class Main {
    public int solution(String str, char ch) {
        int result = 0;
        str = str.toUpperCase();
        ch = Character.toUpperCase(ch);
        for (char s : str.toCharArray()) {
            if (s == ch) result++;
        }
        return result;
        
    }

    public static void main(String[] args) {

        Main m = new Main();
        Scanner sc = new Scanner(System.in);
        String str = sc.next();
        char ch = sc.next().charAt(0);
        System.out.println(m.solution(str, ch));
    }
}
/**
 * 추가 정보 : 위 solution 메서드에서 for문 중 str.toCharArray는 
 * 새로운 배열을 생성하기 떄문에 for(int i=0 ; i<str.length() ; i++)로 하는것이
 * 큰 입력이나 메모리 민감한 환경이면 더 좋다.
 */


```