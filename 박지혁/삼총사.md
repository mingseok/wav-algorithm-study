## 삼총사
```python
from itertools import combinations
def solution(number):
    answer = 0
    for a, b, c in combinations(number, 3):
        if a + b + c == 0:
            answer = answer + 1
    return answer
```

파이썬 공식문서를 보고, itertools 라이브러리의 combinations 메서드 사용.
