### 공백으로 구분하기 1
```java

class Solution {
    public String [] solution(String my_string) {
        String[] answer = {};
        answer = my_string.split(" ");


        return answer;

    }
}

// 인터넷의 도움으로 split이란 변수를 처음 알았습니다
// my_sgting에  i love you가 저장되어 있을때 i,love,you를 스트링 구조에서 list 구조로 끊으면 될줄 알았지만 list로 끊으면 하나의 단어로 끊기기 때문에 list 구조 대신해서 split을 사용했습니다. 
// 공백으로 구분하는 부분에 대해서 다음날 추후에 더 깊게 공부가 필요할것같습니다.
```